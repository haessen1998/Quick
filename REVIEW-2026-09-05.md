# Quick 多维审查报告

审查日期：2026-09-05。范围：当前工作区（包含已有未提交修改），Wails 3 / Go / React 应用代码、AI 与 MCP 链路、工具数据处理、页面交互、构建和测试。此次没有修改应用源码。

## 总体判断

Quick 已有比较完整的开发者工具覆盖，下一阶段最值得投入的是**处理结果可信、工作区连续、AI 操作可控**。继续添加零散工具的收益，低于把现有工具串成稳定工作流。

值得保留的基础：

- 本地优先，密码学、安全随机、文件和网络能力交给 Go，文本处理交给前端，职责总体合理。
- 已有 Wails 类型绑定、页面懒加载、原生文件对话框、文件重命名冲突检测与撤销。
- AI Provider 代理使用随机会话凭据、限制同源目标与跨源重定向；它与下文的 Wails App MCP 是不同入口。
- AI 已有工具步骤展示、按页面获取上下文和确定性工具执行，不只是聊天窗口。
- Go 业务模块已有测试，发布工作流也有测试步骤；问题主要在前端行为测试和日常 PR 门禁。

下面的 P1 表示优先修复，P2 表示近期改进，P3 表示产品演进。优先级同时考虑数据正确性、权限边界和日常使用影响，不是漏洞评分。

## 已验证问题及代码依据

### 1. P1：默认窗口打开小Q后，编辑器布局不可用

**已做页面验证。** 在 1000×618 的浏览器视口中，字符串格式化页同时显示约 240px 应用导航、224px 工具导航和输入/输出两栏。展开默认 336px 的小Q面板后，编辑区被挤成极窄的竖条，按钮换行、文本逐字折行，并出现横向滚动。

原因是工具页的 `md:` 双栏判断使用整个视口宽度，而不是扣除两侧导航后的容器宽度。小Q在 920px 就进入占用横向空间的并排模式。

依据：[默认窗口](D:/Programs/Go/Quick/main.go:74)、[工作区网格](D:/Programs/Go/Quick/frontend/src/components/ToolWorkspace.tsx:146)、[输入输出双栏](D:/Programs/Go/Quick/frontend/src/components/ToolWorkspace.tsx:205)、[小Q面板](D:/Programs/Go/Quick/frontend/src/components/GlobalAssistant.tsx:551)。

建议：按内容容器宽度切换布局；空间不足时工具列表改下拉/搜索，小Q改抽屉，输入输出改标签页或上下排列。增加编辑器专注模式，并缩减重复的大标题和介绍区。验收至少覆盖 1000×618、1280×800、1440×900，以及侧栏开关组合。

### 2. P1：切换工具会无提示覆盖用户输入

**已做页面验证。** 输入 `{"audit_probe":123}` 后点击“JSON 压缩”，输入变回 Quick 内置示例。`selectTool()` 无条件执行 `setInput(tool.sample)`。校验页切换模式也会重新填入样例。

依据：[ToolWorkspace](D:/Programs/Go/Quick/frontend/src/components/ToolWorkspace.tsx:112)、[ValidationPage](D:/Programs/Go/Quick/frontend/src/pages/ValidationPage.tsx:113)。

建议：同一数据文档切换操作时保留输入；示例改成显式“载入示例”按钮。不同工具可以有各自草稿，提供撤销。无需为了保留编辑状态永久挂载所有页面。

### 3. P1：JSON 格式化和转换会改变大整数

**已用同一解析链路验证。** `{"id":9223372036854775807}` 经 `JSON.parse` / `JSON.stringify` 后变为 `{"id":9223372036854776000}`。对 Go int64、数据库主键、雪花 ID 等常见数据，这是正确性问题。单纯格式化不应改变数值。

依据：[JSON 格式化和压缩](D:/Programs/Go/Quick/frontend/src/pages/StringToolsPage.tsx:79)、[数据解析](D:/Programs/Go/Quick/frontend/src/pages/DataConversionPage.tsx:102)。

建议：格式化采用保留数字原文的 token/语法树处理；跨格式转换采用显式的数字精度策略，遇到不支持的范围给出提示。测试超出安全整数范围的数字、科学计数法和高精度小数。不要直接把所有大数转成字符串而不告知用户，这也会改变类型。

### 4. P1：CSV 自动推断类型破坏前导零

**已使用项目安装的 PapaParse 验证。** 输入 `zip,code\n00123,00007`，当前参数得到 `[{"zip":123,"code":7}]`。邮编、编号、账号等不能默认按数值理解。

依据：[dynamicTyping: true](D:/Programs/Go/Quick/frontend/src/pages/DataConversionPage.tsx:107)。

建议：默认保留文本，类型推断作为可选项；增加列级转换预览和类型覆盖。与 JSON 精度问题一起作为“数据保真”专项处理。

### 5. P1：配置加密使用全体安装共享的固定密钥

**代码确认。** `configEncryptionPassword = "quick"`，再用 SHA-256 派生 AES-GCM 密钥。随机 nonce 能避免重复密文，但不能补救公开固定密钥。获得配置文件的人可以按源码解密 AI 和 MCP 配置；这不等于系统级凭据保护。

依据：[固定密码与派生](D:/Programs/Go/Quick/internal/config/configservice.go:20)、[Load 返回解密内容](D:/Programs/Go/Quick/internal/config/configservice.go:228)。

建议：普通配置与秘密分离；使用 Windows/macOS/Linux 对应的系统凭据设施，配置只保存引用。迁移时验证新存储成功后再清理旧数据。系统凭据设施不可用时明确提示，避免悄悄写回明文。

边界：系统凭据存储主要改善静态文件泄露风险，不能抵御同权限进程调用一个可读取秘密的应用接口，因此必须同时处理下一项。

### 6. P1：发布包默认携带高权限 Wails App MCP

**项目构建配置与本地依赖源码确认，未调用破坏性方法。** Windows/macOS 构建默认加入 `mcp` 标签。所使用的 Wails beta.16 实现自动启动 MCP，允许调用 `call_bound_method`，可触达应用已绑定的 Go Service。

本地依赖的 MCP HTTP handler 有 Origin 检查，但没有请求身份认证；无 Origin、`null` Origin 和本机 Origin 被接受。不能把监听回环地址视为调用者授权。Go `ConfigService.Load` 能返回解密配置，`MCPStdioService.Connect` 能启动命令；这些能力不应通过通用调试入口默认交给任意本机调用者。

依据：[Windows 构建](D:/Programs/Go/Quick/build/windows/Taskfile.yml:32)、[macOS 构建](D:/Programs/Go/Quick/build/darwin/Taskfile.yml:29)、[Service 注册](D:/Programs/Go/Quick/main.go:49)、[STDIO 启动](D:/Programs/Go/Quick/internal/mcp/mcpstdioservice.go:82)。依赖证据为本机模块缓存中的 `pkg/application/mcp_protocol_enabled.go:64` 和 `mcp_tools_enabled.go:438`，结论仅对应当前版本。

建议：将调试 MCP 与产品 MCP 分开。正式包默认关闭通用 DOM/绑定方法调试入口；如需让外部 AI 操作 Quick，提供经过认证的专用工具白名单、明确授权范围和撤销机制。仅增加前端确认框或 system prompt 无法约束绕过前端的调用。

这里确认的是入口与权限边界问题；没有在恶意网站、其他系统用户或实际发布安装包上进行攻击验证。

### 7. P1：用户正则在 UI 线程同步运行，可阻塞整个页面

`replacementPreview` 和高亮在 `useMemo` 中执行 `RegExp`，每次输入变化都会运行，甚至不需要点“执行”。JavaScript 正则遇到回溯模式可能长时间占用线程，`try/catch` 不解决执行时间问题。

**隔离验证：** `(a+)+$` 对 30 个 `a` 后接 `!` 的输入，在 Node 隔离 Worker 中超过 1 秒未完成，随后终止 Worker。未在用户页面制造卡死；WebView 上具体耗时未测量。

依据：[正则高亮](D:/Programs/Go/Quick/frontend/src/pages/ValidationPage.tsx:81)、[同步预览](D:/Programs/Go/Quick/frontend/src/pages/ValidationPage.tsx:131)。

建议：放入可终止的 Worker，增加超时、输入与结果上限、取消按钮。仅加防抖不能解决一次执行卡死。JSON Schema、文本 diff、较大格式化输入也应采用统一任务执行设施。

### 8. P2：中文输入法确认词语时可能触发发送

**代码确认，未在原生 IME 中复现。** 普通聊天和小Q只判断 `Enter && !shiftKey`，没有排除 composition。对中文为默认语言的应用，这是高频交互边界。

依据：[普通聊天](D:/Programs/Go/Quick/frontend/src/pages/AIChatPage.tsx:169)、[小Q聊天](D:/Programs/Go/Quick/frontend/src/components/GlobalAssistant.tsx:417)。

建议：抽出共享输入组件，统一处理 composition 状态、发送快捷键、失败草稿恢复，并在 Windows/macOS 输入法下验收。

### 9. P2：HTML 压缩可能改变文本语义

`minifyHtmlText()` 用正则删除所有标签间空白。例如 `<span>Hello</span> <span>world</span>` 会失去两个内联元素之间的可见空格；同样不理解 `pre` 等保留空白的语境。

依据：[minifyHtmlText](D:/Programs/Go/Quick/frontend/src/pages/StringToolsPage.tsx:48)。

建议：使用理解 HTML 上下文的压缩流程；将“输出前后可见文本保持一致”作为回归条件。当前说明“不破坏文本内容”过于绝对。

## 架构与工程改进

### A. 将业务能力从 React 页面里拆出来

当前 `GlobalAssistant` 会先导航，再通过 `AssistantCapabilityProvider` 等待页面注册动作，最长等 5 秒。也就是说，业务能否执行取决于 UI 是否挂载。

依据：[AI 路由](D:/Programs/Go/Quick/frontend/src/components/GlobalAssistant.tsx:103)、[能力注册表](D:/Programs/Go/Quick/frontend/src/lib/assistant-capabilities.tsx:32)。

建议建立统一工具描述：`id / inputSchema / outputSchema / effects / execute / present`。页面按钮、命令面板和 AI 调用同一业务动作；展示结果时才决定是否跳页。纯文本工具保留 TypeScript 实现，不必为了统一而全部搬去 Go；涉及系统权限的最终检查放在 Go 服务边界。

目前工具枚举、页面 labels、能力清单、模型 schema 分布多处，新增功能容易漏同步。统一注册表可派生导航、AI schema、搜索项与帮助文档。

### B. 控制页面生命周期与不必要重建

`PageSlot` 通过 `hidden` 保留所有访问过的页面；根组件时钟每秒更新，`enabledMCPServers = mcpServers.filter(...)` 每次产生新数组，进而使小Q transport 的 memo 依赖变化。这是可见的额外重建路径，但本次没有做 CPU/RSS profiling，不能给出内存泄漏或耗电结论。

依据：[PageSlot](D:/Programs/Go/Quick/frontend/src/App.tsx:113)、[根状态和时钟](D:/Programs/Go/Quick/frontend/src/App.tsx:665)、[transport 依赖](D:/Programs/Go/Quick/frontend/src/components/GlobalAssistant.tsx:389)。

建议：将时钟状态下沉、稳定派生数组，页面只保存草稿数据；为后台任务、预览和 MCP 连接定义明确生命周期，再按数据量决定缓存数量。先测量，再优化。

### C. 移除运行时 DOM 翻译机制

当前国际化通过 `MutationObserver` 扫描 body，直接修改 React 管理的文本节点和属性，同时用 WeakMap 保存原文。这增加额外扫描，并带来动态文本恢复错误、用户内容误翻译和 React 与 DOM 状态不一致的维护风险。

依据：[DOM 本地化](D:/Programs/Go/Quick/frontend/src/lib/i18n.tsx:672)、[全局观察器](D:/Programs/Go/Quick/frontend/src/lib/i18n.tsx:746)。

建议：逐步改为组件渲染时使用稳定消息 key 和参数插值；用户文本、工具结果与日志独立呈现。先迁移共享组件和设置页，再迁移工具页，不需要一次重写所有文案。

### D. MCP 连接按工作会话复用

`listSavedMCPTools` 与 `callSavedMCPTool` 每次建立连接并在 finally 关闭；STDIO 每次都会重新启动进程。对于依赖服务端内存状态、资源句柄或初始化成本较高的服务器，发现工具和执行工具可能发生在不同实例。

依据：[远端连接](D:/Programs/Go/Quick/frontend/src/lib/mcp-assistant-client.ts:42)、[STDIO 会话](D:/Programs/Go/Quick/frontend/src/lib/mcp-assistant-client.ts:85)。

建议：共用 MCPConnectionManager，以配置版本、transport 和会话为边界复用，支持空闲回收、重连、取消及 schema 缓存失效。合并 MCP Inspector 和小Q的连接基础设施，界面职责仍可保留。

### E. 配置保存需要可见状态和恢复路径

AI/MCP 持久化失败仅 `console.warn`，初始化失败后会使用 localStorage，且保存逻辑保留上一版 backup。这既使用户难以判断是否保存成功，也可能使敏感字段落在 WebView 存储中。主题、代理、助手设置与 AI/MCP 配置又分属不同持久化介质。

依据：[初始化与保存](D:/Programs/Go/Quick/frontend/src/App.tsx:713)、[localStorage 保存](D:/Programs/Go/Quick/frontend/src/lib/saved-connections.ts:240)、[代理配置](D:/Programs/Go/Quick/frontend/src/lib/proxy.ts:26)。

建议：统一版本化设置服务；区分 loading/ready/failed 状态，保存失败显示 Toast 与重试。敏感数据不使用静默明文 fallback；普通偏好可缓存。异步加载完成前不要覆盖用户刚编辑的值。

### F. 增加日常质量门禁，而非只在打包时测试

前端目前只有 build/dev/preview 脚本，没有发现自有前端行为测试文件。现有两个 CI 工作流由 tag 或手动触发，已有 Go 测试步骤，但没有 PR 检查入口。

建议：PR 执行类型检查、构建、Go test/vet，补少量高价值测试：大整数、CSV 前导零、模式切换保留数据、正则取消、IME、MCP 授权和设置迁移。前端增加 ErrorBoundary，将页面加载失败与正常 loading 区分开。逐步启用 `noImplicitAny`，并拆分 App、GlobalAssistant 和 MCPInspector 的设置/协议/视图职责。

构建输出存在约 1.02MB 和 1.52MB 的压缩后 JS chunk（gzip 分别约 252KB、468KB）。这是需要检查加载边界的信号，**不是已测得启动慢**。页面已经懒加载，后续应检查全局 AI 与 Markdown 依赖何时进入首屏，避免为消除警告机械拆包。

## AI 产品设计

1. **普通聊天与小Q共享会话底座。** 保留“问答”和“操作 Quick”两个模式，复用 Provider、输入框、会话列表、Markdown、错误恢复与用量显示。当前两套聊天实现已出现重复的 IME 问题。
2. **用数据引用串联动作。** 工具结果存为本地 artifact，AI 使用 resultId 将 HTTP 响应交给 JSONPath、diff 等动作。避免大段数据反复经过模型以及当前多处 slice 截断造成信息不完整。
3. **上下文可见。** 输入区展示本次附带哪些页面数据、截取了多少、发送到哪个 Provider。对 Authorization、Cookie、令牌、私钥及可疑敏感字段统一脱敏；“本地工具”与“发送给模型”应在 UI 中容易区分。
4. **审批细化。** 将单个 autoApproveOperations 拆成只读、网络请求、文件修改、进程操作、第三方 MCP 等范围；支持本次/会话级授权。高风险工具在执行边界验证，不能仅依赖提示词要求模型理解“用户明确要求”。
5. **结果必须能核验。** 统一 success/error/cancelled、耗时、变更摘要和结果入口；停止生成要能传递到长任务及 MCP 调用。对文件操作保留计划与撤销记录。
6. **会话连续性与预算。** 增加本地可选会话保存、切换模型时的历史保留策略、上下文用量及超限处理。当前 `stepCountIs(12)` 只限制步骤数，不等于限制会话总成本或历史长度。
7. **建立 AI 回归用例。** 用固定样例评估选工具正确率、结果正确性、危险操作是否被阻止、取消是否生效；不必一开始做复杂平台。

## 功能融合建议

| 融合方向 | 现有能力 | 用户得到的完整任务 |
|---|---|---|
| 数据工作台 | 格式化、转换、JSONPath、Schema、diff | 同一份数据清洗、转换、验证、比较，全程保留原文 |
| API 调试工作台 | URL/Query、HTTP/cURL、JSON、JWT、网络诊断 | 发请求、看响应、提取字段、比较两次结果、让 AI 解释失败 |
| 文本与文件批处理 | 文本清理、正则、文件摘要、重命名 | 先预览规则和影响，再批量处理、保存记录、撤销 |
| 时间与生成器 | 时间戳、多时区、Cron、ID | 快速查询和可重复的样例数据生成，可从命令面板直接调用 |
| MCP 工作区 | Inspector、小Q的 MCP 客户端、保存连接 | 共用连接与调用历史，支持发现、试调用、审批和诊断 |

“融合”优先体现为共享数据、执行设施和下一步入口，不必把所有功能强行塞到一个巨型页面。站点导航可以作为独立辅助页，优先级低于上述工作流。

## 值得新增的功能及顺序

**优先新增：**

- 全局命令面板：支持工具名、别名、最近使用与收藏，一次输入直达“Base64 解码”等具体操作。
- 文档/结果标签页与草稿恢复：同一工具处理多份输入，切换操作不丢内容；敏感工具可关闭历史记录。
- “发送到…”：任意输出直接交给转换、校验、diff 或 AI，减少复制粘贴。
- 统一编辑器：行号、语法高亮、错误定位、查找、换行开关、专注模式。先按实际使用选择轻量方案。

**底座稳定后新增：**

- 可保存的操作流程：例如 HTTP → JSONPath → CSV；先做顺序流程，加入结果引用、逐步预览和副作用确认。
- HTTP 环境变量与请求集合：与现有 Provider/MCP 秘密存储共享凭据设施，避免出现第四套凭据管理。
- JSON/Schema 驱动的样例数据生成、字段类型映射及更可靠的代码模型生成。
- 配置导入导出、迁移和恢复，但秘密默认排除，明确处理跨设备凭据。

**暂缓：** 插件市场、云同步、多人协作、向量库/RAG、更多 AI Provider、继续堆叠独立转换页面。当前代码没有显示这些能力比工作区和数据可靠性更紧迫。

## 建议实施顺序与验收

1. **可靠性修复批次：** 大整数与 CSV 保真、输入保留、HTML 语义、正则隔离、IME、默认窗口布局。验收用上面的最小复现样例，加窗口开关组合。
2. **权限和配置批次：** 正式包 MCP 边界、系统凭据存储、失败提示和配置迁移；验证不能通过替代入口绕开授权。
3. **架构批次：** 工具执行层、任务取消、草稿/结果存储、统一聊天与 MCP 会话管理；业务执行不再要求先挂载页面。
4. **体验和增长批次：** 命令面板、编辑器、发送到下一工具、可保存工作流；用真实任务完成时间和错误率评估收益。

不建议先全面重写 UI 或一次迁移整个技术栈。可以按这四批渐进推进，每批都交付可观察的收益。

## 本次验证与限制

- `npm run build --prefix frontend`：通过；出现 chunk 大小警告，以及 npm 不识别项目 `minimum-release-age` 配置的警告。未重新安装依赖。
- `go vet -tags mcp ./...`：通过。
- `go test -tags mcp ./...`：AI、配置、加密、文件、MCP、导航包通过（结果来自 Go 缓存）；network 包两项查询进程测试失败。直接运行 `tasklist` 返回 Access denied，说明当前环境权限影响执行，不能据此宣称应用进程查询逻辑错误。
- 浏览器：复用已有 43121 本地预览，创建临时页面，验证 1000×618 布局、展开小Q、切换工具丢失输入；完成后关闭临时页面并恢复视口。
- Node 隔离检查：验证大整数精度、PapaParse 前导零行为和正则执行超时。
- 未调用外部 AI Provider，未发送真实 HTTP 业务请求，未启动第三方 MCP，未终止进程或重命名用户文件；没有对原生 WebView、IME、macOS/Linux 和正式安装包做端到端验证。
- 浏览器界面与原生窗口可能存在平台差异；已验证的布局问题发生于项目默认尺寸的网页内容区。
- 外部 Wails 文档页面未成功读取；依赖行为结论基于当前本地 beta.16 源码，不推断其他版本。
- 保留了审查开始前已有的工作区修改；新增文件仅为此报告，构建重新生成了被忽略的前端产物。
